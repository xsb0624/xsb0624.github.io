# 博客管理手册

> 你的博客已经搭建完成并上线：**https://yizhixiong.com/**
>
> 本文档教你如何在电脑上轻松写作和发布文章。

---

## 一、日常发文章：三步搞定

1. **新建文章**：双击 `新建文章.bat`，输入标题，回车。会自动创建一篇带日期的新文章并打开编辑器。
2. **写内容**：在打开的编辑器里用 Markdown 格式写正文（下面有格式示例）。
3. **发布上线**：写完保存后，双击 `发布博客.bat`，等待提示"发布成功"，1-2 分钟后刷新博客即可看到新文章。

> 就这么简单。不需要懂 Git，不需要命令行操作。

---

## 二、本机上的三个快捷工具

| 文件 | 作用 |
|---|---|
| `新建文章.bat` | 一键创建新文章（自动命名、自动加日期） |
| `预览博客.bat` | 在本地浏览器预览效果（不影响线上） |
| `发布博客.bat` | 构建 + 自动上传，一键发布到 GitHub |

> **小提示**：可以把这三个 .bat 文件发送到桌面快捷方式，方便随手使用。

---

## 三、Markdown 写作格式（简单够用）

文章内容放在 `content/posts/` 目录，每篇文章是一个 `.md` 文件，开头有一段"配置信息"（front matter）：

```markdown
---
title: "文章标题"          # 标题
date: 2026-08-25T10:00:00  # 发布时间
draft: false               # false=发布, true=草稿(不显示)
description: "文章简介"     # 可选，列表页显示
tags: ["标签1", "标签2"]    # 可选，方便分类
categories: ["分类"]        # 可选
---

正文从这里开始，用 Markdown 写：

# 一级标题
## 二级标题

**加粗** 和 *斜体*

- 列表项1
- 列表项2

> 引用文字

[链接文字](https://example.com)

![图片说明](/images/xxx.jpg)

`代码片段`

```代码块（用三个反引号包起来）
```

---

## 四、添加图片（推荐：粘贴即上传）

博客已配置好 **Obsidian 粘贴图片自动上传** 工作流：

**方法一（推荐，配合 Obsidian）**
1. 在 Obsidian 中打开要写的文章
2. 直接 **Ctrl+V 粘贴截图**，或把图片文件**拖进**文章里
3. 图片会自动保存到博客的 `images/` 文件夹，并自动生成 `/images/文件名.png` 链接
4. 保存文章，双击 `发布博客.bat` 上线即可

**方法二（手动放图）**
1. 把图片文件直接放进博客的 `images/` 文件夹
2. 在文章里写：`![图片说明](/images/你的文件名.png)`

> 提示：
> - 图片文件名建议用英文或数字，如 `shop-photo-01.png`（不要用中文和空格）
> - 单张图片控制在 2MB 以内，太大了会拖慢页面（可用压缩工具缩小）
> - 首次使用 Obsidian 前，请先完全关闭再重新打开，让它读取新的图片配置

---

## 五、推荐使用 Obsidian 写作（已安装）

Obsidian 是免费的 Markdown 编辑器，界面舒适、所见即所得。

1. 打开 Obsidian
2. 选择 **"打开文件夹作为仓库"（Open folder as vault）**
3. 选择博客文件夹：`C:\Users\Administrator\Doubao\chats\2026-08-25\new-chat\myblog`
4. 之后在左侧文件列表中找到 `content/posts/` 下的文章，点击即可编辑

> 用 Obsidian 打开后，写作体验更好（左侧目录、右侧实时预览）。

---

## 六、GitHub Desktop（可选，用于查看版本记录）

已安装 GitHub Desktop，如果想知道博客的修改历史：

1. 打开 GitHub Desktop
2. 添加本地仓库 → 选择博客文件夹 `...\myblog`
3. 可以看到每次发布的记录，也能手动查看代码变化

> 日常发布不需要打开它，`发布博客.bat` 已自动处理。

---

## 七、博客文件结构速览

```
myblog/
├── 发布博客.bat        ← 一键发布（用这个）
├── 新建文章.bat        ← 新建文章（用这个）
├── 预览博客.bat        ← 本地预览（用这个）
├── publish.ps1 / new-post.ps1 / preview.ps1   ← 上面的后台脚本，不用动
├── hugo.toml           ← 博客设置（标题、菜单等）
├── content/
│   ├── posts/          ← ★ 你的文章都放这里
│   ├── about.md        ← "关于"页面
│   ├── search.md       ← 站内搜索页
│   └── archives.md     ← 归档页
├── images/             ← ★ 文章配图(Obsidian 粘贴图片自动存这里)
├── static/
│   └── CNAME           ← 自定义域名配置文件
└── themes/PaperMod/    ← 主题文件（一般不用动）
```

---

## 八、博客设置（进阶，可选）

打开 `hugo.toml` 可以修改：

- `title`：博客名称
- `homeInfoParams.Content`：首页欢迎语
- `socialIcons`：底部社交链接
- 菜单（首页/归档/标签/搜索/关于）：可增删

改完保存后，运行 `发布博客.bat` 即可生效。

---

## 九、常见问题

**Q: 发布后多久能看到更新？**
约 1-2 分钟，GitHub 自动构建部署。

**Q: 想写草稿不发布怎么办？**
把文章开头的 `draft: false` 改成 `draft: true`，就不会显示在线上。

**Q: 发布失败提示网络问题怎么办？**
你所在网络连接 GitHub 不太稳定，稍等片刻重新双击 `发布博客.bat` 即可。

**Q: 想改文章标题/标签？**
直接打开那篇文章的 .md 文件，修改开头 `title` / `tags` 后重新发布。

**Q: 误删了文章能找回吗？**
能。打开 GitHub Desktop，右键历史记录即可找到旧版本；GitHub 网页仓库页也可查看历史。

---

## 十、账号与安全

- **GitHub 账号**：xsb0624@qq.com（用户名 xsb0624）
- **访问令牌**：名为 `hugo-blog-deploy`，已安全保存在 **Windows 凭据管理器** 中，发布时间到期日为 **2026-11-23**
  - 到期后若发布失败，打开 https://github.com/settings/tokens 重新生成（勾选 repo 和 workflow 权限），并告诉我帮你重新配置即可
- 请在 GitHub 网页端（Settings → Security）确认开启了两步验证，保护账号安全

---

## 十一、备用：用 GitHub 网页直接管理

如果不在自己电脑上，也可以：
1. 打开 https://github.com/xsb0624/xsb0624.github.io
2. 进入 `content/posts/` 文件夹
3. 点 "Add file" → "Create new file" 新建文章（注意保留文件开头的配置信息）
4. 提交后 GitHub 会自动构建上线
